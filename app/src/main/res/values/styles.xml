<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:clipToPadding">true</item>
        <item name="android:windowContentTransitions">true</item>

    </style>

    <style name="AppThemeDark" parent="Theme.AppCompat" />
    <style name="AppThemeLight" parent="Theme.AppCompat.Light" />
    <style name="AppThemeLight.DarkActionBar" parent="Theme.AppCompat.Light.DarkActionBar" />
    <style name="BlackCircleProgressBar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateDrawable">@drawable/progress_circle_black_anim</item>
    </style>

</resources>
